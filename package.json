{
  "name": "recipe-book",
  "version": "1.0.1",
  "description": "Digital Recipe Book",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate-default-images": "node scripts/generate-default-images.mjs",
    "prepare-chef-masters": "node scripts/prepare-chef-masters.mjs",
    "generate-app-icons": "node scripts/generate-app-icons.mjs",
    "deploy:functions": "npx supabase functions deploy recipe-ai regenerate-image --project-ref nklwzunoipplfkysaztl --use-api --import-map supabase/functions/deno.json",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist && git commit -m \"chore: rebuild dist\" && git push origin main"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.0",
    "vite": "^6.4.3",
    "vite-plugin-pwa": "^0.21.1"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
